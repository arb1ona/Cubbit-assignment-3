import React, { ReactElement } from 'react'

interface Props {
    
}

export default function BillingInfo({}: Props): ReactElement {
    return (
        <div>
            BillingInfo
        </div>
    )
}

import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ContactInfo({}: Props): ReactElement {
    return (
        <div>
            ContactInfo
        </div>
    )
}

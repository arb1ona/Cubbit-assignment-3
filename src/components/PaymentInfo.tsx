import React, { ReactElement } from 'react'

interface Props {
    
}

export default function PaymentInfo({}: Props): ReactElement {
    return (
        <div>
            PaymentInfo
        </div>
    )
}

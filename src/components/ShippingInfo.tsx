import React, { ReactElement } from 'react'

interface Props {
    
}

export default function ShippingInfo({}: Props): ReactElement {
    return (
        <div>
            ShippingInfo
        </div>
    )
}

import React, { ReactElement } from 'react'

interface Props {
    
}

export default function CheckoutPage({}: Props): ReactElement {
    return (
        <div>
            asenfanefa
        </div>
    )
}
